# 📦 دليل بناء التطبيق (APK/IPA)

## 🎯 الطرق المتاحة

### ✅ الطريقة الموصى بها: EAS Build (سهلة ومجانية)
### ⚡ الطريقة المحلية: Android Studio (للخبراء)

---

## 🚀 الطريقة 1: EAS Build (الأفضل)

### المميزات:
- ✅ مجاني تماماً
- ✅ بناء في السحابة
- ✅ بدون Android Studio/Xcode
- ✅ APK + AAB جاهزين

---

### الخطوات:

#### 1️⃣ تثبيت EAS CLI

```bash
npm install -g eas-cli
```

#### 2️⃣ تسجيل الدخول

```bash
eas login
```

إذا ليس لديك حساب:
```bash
# افتح المتصفح
https://expo.dev/signup

# بعدها:
eas login
```

#### 3️⃣ Configure المشروع

```bash
eas build:configure
```

سيسألك بعض الأسئلة:
- Platform: اختر `Android` أو `All`
- What would you like your Android package name to be?: `com.deliverease.app`

#### 4️⃣ Build للأندرويد (APK)

```bash
# APK للتنصيب المباشر
eas build -p android --profile preview

# أو AAB للرفع على Play Store
eas build -p android --profile production
```

#### 5️⃣ انتظر البناء (10-15 دقيقة)

سيظهر لك:
```
✔ Build started, it may take a few minutes to complete.

Build URL: https://expo.dev/...
```

#### 6️⃣ تحميل APK

بعد انتهاء البناء:
1. اضغط على الـ link
2. Download الـ APK
3. انقله لموبايلك
4. نصبه!

---

## 🔧 الطريقة 2: بناء محلي (Advanced)

### متطلبات:
- Android Studio
- Java JDK 17
- مساحة 10+ GB

### الخطوات:

#### 1️⃣ Generate Native Project

```bash
npx expo prebuild --platform android
```

#### 2️⃣ Build APK

```bash
cd android
./gradlew assembleRelease
```

#### 3️⃣ APK Location

```
android/app/build/outputs/apk/release/app-release.apk
```

---

## 📱 iOS Build (يتطلب Mac)

### Using EAS:

```bash
eas build -p ios --profile preview
```

### متطلبات:
- Apple Developer Account ($99/سنة)
- Mac (للبناء المحلي)

---

## ⚙️ ملفات الـ Config المهمة

### 1. app.json

```json
{
  "expo": {
    "name": "DeliverEase",
    "slug": "deliverease",
    "version": "1.0.0",
    "android": {
      "package": "com.deliverease.app",
      "versionCode": 1
    },
    "ios": {
      "bundleIdentifier": "com.deliverease.app",
      "buildNumber": "1.0.0"
    }
  }
}
```

### 2. eas.json (Already exists!)

```json
{
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "distribution": "internal",
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "android": {
        "buildType": "aab"
      }
    }
  }
}
```

---

## 🎯 Quick Build Commands

```bash
# Android APK (للتجربة)
eas build -p android --profile preview

# Android AAB (للنشر)
eas build -p android --profile production

# iOS (IPA)
eas build -p ios --profile preview

# Both platforms
eas build --platform all
```

---

## 📊 Build Profiles

| Profile | Type | Use Case |
|---------|------|----------|
| **development** | Dev Build | Development + Debugging |
| **preview** | APK/IPA | Internal Testing |
| **production** | AAB/IPA | Play Store / App Store |

---

## 🔐 تجهيز Production Build

### 1. Update Version

في `app.json`:
```json
{
  "version": "1.0.0",
  "android": {
    "versionCode": 1  // Increment for each build
  }
}
```

### 2. Configure Credentials

```bash
# Android Keystore (auto-generated by EAS)
eas credentials

# iOS Certificates (requires Apple Developer)
eas credentials
```

### 3. Build

```bash
eas build -p android --profile production
```

---

## 📱 تنصيب APK على الموبايل

### Android:

1. انقل APK للموبايل
2. Settings → Security → Install from Unknown Sources ✅
3. File Manager → Downloads
4. اضغط على APK → Install

### أو عن طريق ADB:

```bash
adb install app-release.apk
```

---

## 🆘 المشاكل الشائعة

### Problem: "eas command not found"

```bash
npm install -g eas-cli
# أو
npm install -g eas-cli --force
```

### Problem: "Build failed"

```bash
# Check logs
eas build:list

# View specific build
eas build:view [BUILD_ID]
```

### Problem: "Keystore not found"

```bash
# Generate new keystore
eas credentials
```

---

## 🎊 Build Sizes

**Expected Sizes:**
- APK: ~50-80 MB
- AAB: ~40-60 MB
- IPA: ~60-100 MB

---

## 🚀 Next Steps بعد Build

1. ✅ Test APK على موبايل
2. ✅ Fix أي bugs
3. ✅ Update version
4. ✅ Build production (AAB)
5. ✅ Upload to Play Store

---

## 📖 روابط مفيدة

- EAS Build Docs: https://docs.expo.dev/build/introduction/
- Play Console: https://play.google.com/console
- App Store Connect: https://appstoreconnect.apple.com/

---

## ⚡ Quick Start (الأسرع!)

```bash
# 1. Install EAS
npm install -g eas-cli

# 2. Login
eas login

# 3. Build
eas build -p android --profile preview

# 4. Wait 10-15 minutes
# 5. Download APK
# 6. Install on phone!
```

---

**Status: Ready to Build! 🎉**

**Recommended:** `eas build -p android --profile preview`
